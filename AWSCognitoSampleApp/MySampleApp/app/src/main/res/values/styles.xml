<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/primaryDark</item> <!-- this changes the status bar color on the phone to match our APP theme -->
        <item name="colorPrimary">@color/primaryDark</item> <!-- this changes top bar color on the activities containers -->
        <!--
        <item name="android:navigationBarColor">@color/primaryDark</item> : this could be use to change the bottom victual navigation bar on the phone ,
                                                                            if we have an API level over 22 with the "android:Theme.Material" as the theme of our app
        -->
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

</resources>
